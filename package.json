{
  "name": "reactiveobj",
  "version": "1.0.1",
  "description": "Watch an object and react if a property changes",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "browserify index-module.js -t babelify -o index.js -s Observable"
  },
  "keywords": [
    "observer",
    "reactive",
    "observer-pattern"
  ],
  "repository": "https://github.com/mukesh-urbanpiper/observableObj",
  "author": "Mukesh",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.2.2",
    "@babel/preset-env": "^7.3.1",
    "babelify": "^10.0.0"
  }
}
